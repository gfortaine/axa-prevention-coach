{
  "name": "@client-ai-pocs/axa-prevention-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Python LangGraph Agent Server service for the AXA prevention coach POC.",
  "scripts": {
    "dev": "uv run langgraph dev --no-browser",
    "lint": "uv run ruff check .",
    "format:check": "uv run ruff format --check .",
    "typecheck": "uv run pyright",
    "test": "uv run pytest"
  }
}
